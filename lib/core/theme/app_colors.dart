import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand
  static const primary = Color(0xFF4D41DF);
  static const secondary = Color(0xFF005BBE);
  static const tertiary = Color(0xFF7ADFA1);

  // Base surfaces
  static const surface = Color(0xFFFCF8FF);
  static const surfaceContainerLowest = Color(0xFFFFFFFF);
  static const surfaceContainerLow = Color(0xFFF5F0FB);
  static const surfaceContainer = Color(0xFFEDE7F7);
  static const surfaceContainerHigh = Color(0xFFE7E0F4);
  static const surfaceContainerHighest = Color(0xFFF3EEFA);
  static const surfaceVariant = Color(0xFFF0EBF7);

  // Text
  static const onSurface = Color(0xFF1B1B24);
  static const onSurfaceVariant = Color(0xFF6E7086);
  static const textHint = Color(0xFFA0A3B8);

  // Border / outline
  static const outlineVariant = Color(0xFFD9D3E6);

  // States
  static const success = Color(0xFF35C98D);
  static const correctContainer = Color(0xFFE9FAF0);
  static const error = Color(0xFFD64545);
  static const warning = Color(0xFFFFB547);

  // Social
  static const googleSurface = Colors.white;
  static const appleSurface = Color(0xFF15161B);

  // Blur accents
  static const blurPurple = Color(0x664D41DF);
  static const blurBlue = Color(0x4D005BBE);
  static const blurGreen = Color(0x4035C98D);
}